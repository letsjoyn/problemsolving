class Solution {
public:
    int reverse(int x) {
        long long rem=0;
       while(x!=0){
        rem=(rem*10)+(x%10);
        x/=10;
       } 
       if(rem>INT_MIN && rem<INT_MAX) return rem;
       else return 0;
    }
};
